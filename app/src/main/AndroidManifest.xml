<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.netology.diplom_work">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="ANDROID.PERMISSION.READ_EXTERNAL_STORAGE"/>

    <application
        android:name="ru.netology.diplom_work.utils.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_recipe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_recipe_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewPrescription">
        <activity
            android:name="ru.netology.diplom_work.demo.userInterface.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>